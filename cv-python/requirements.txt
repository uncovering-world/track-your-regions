# fastapi >=0.124.0 is the minimum that allows starlette>=0.49.1, which closes
# GHSA-2c2j-9gv5-cj73 (fixed in starlette 0.47.2) and GHSA-7f5h-v6xp-fcq8
# (fixed in starlette 0.49.1). Upgrade if newer GHSAs land via pip-audit.
fastapi==0.124.0
starlette==0.49.1
# uvicorn[standard] >=0.46.0 transitively pulls h11>=0.16.0 (closes the
# malformed-chunked-encoding CVE in older h11).
uvicorn[standard]==0.46.0
numpy==2.2.4
opencv-python-headless==4.11.0.86
scikit-image==0.25.2
scipy==1.14.1
# python-multipart >=0.0.27 closes DoS-via-large-preamble, unbounded headers
# DoS, and arbitrary file write CVEs from the 0.0.20 series.
python-multipart==0.0.27
easyocr==1.7.2
# segment-anything==1.0 dropped: pulls torch 1.8.1 (2021) with multiple critical
# RCE/heap-overflow CVEs and no available patch path. Add back in rebuild/41-43
# from a maintained fork (e.g. ultralytics-sam) when actually used.
pyproj==3.7.1
