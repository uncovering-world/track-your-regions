{
  "name": "track-your-regions-ui",
  "version": "1.0.0",
  "description": "UI for the Track Your Regions project",
  "main": "index.js",
  "scripts": {
    "start": "react-scripts --openssl-legacy-provider start",
    "build": "react-scripts --openssl-legacy-provider build",
    "test": "react-scripts --openssl-legacy-provider test",
    "eject": "react-scripts --openssl-legacy-provider eject",
    "lint": "eslint . --ext .js,.jsx --ext .md",
    "lint:fix": "eslint . --ext .js,.jsx --ext .md --fix"
  },
  "dependencies": {
    "@emotion/react": "^11.11.1",
    "@emotion/styled": "^11.11.0",
    "@mui/material": "^5.14.13",
    "axios": "^1.5.1",
    "maplibre-gl": "^3.5.2",
    "prop-types": "^15.8.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "@turf/turf": "^6.5.0"
  },
  "overrides": {
    "glob": "10.3.10"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "eslint-config-airbnb": "^19.0.4",
    "eslint-plugin-markdownlint": "^0.5.0",
    "react-scripts": "^5.0.1"
  }
}
